# V6.1 Expected Output

Only `Sheet1` is used.

Expected behavior:

- Existing clean official jobs stay in `Sheet1`.
- More public-source jobs are added when found.
- LinkedIn URLs are not used as final apply URLs.
- Public job-board detail URLs can be used for quantity.
- Fake pages, service pages, generic search pages, content pages, and duplicates are removed.

Target after first few runs:

- 7 verified official jobs should remain.
- Additional public-source jobs should start increasing the count.
- Old V4/V5 tabs should be deleted if the service account has permission.
